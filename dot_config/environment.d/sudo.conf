# GUI password prompt for sudo when no TTY is available (e.g. from agents/scripts).
# When a TTY exists (SSH, terminal), sudo prompts normally in the terminal.
SUDO_ASKPASS=$HOME/.local/bin/sudo-askpass
