[Unit]
Description=Noctalia Shell (auto-restart on crash)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/qs -c noctalia-shell
Restart=always
RestartSec=2

[Install]
WantedBy=graphical-session.target
