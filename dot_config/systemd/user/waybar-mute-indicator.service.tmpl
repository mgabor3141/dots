[Unit]
PartOf=graphical-session.target
After=graphical-session.target
Requisite=graphical-session.target

[Service]
ExecStart=/usr/bin/waybar -b mute --config {{ .chezmoi.destDir }}/.config/waybar-mute-indicator/config --style {{ .chezmoi.destDir }}/.config/waybar-mute-indicator/style.css
Restart=on-failure

[Install]
WantedBy=graphical-session.target
