#!/bin/bash
trap 'echo "❌ Error on line $LINENO: $BASH_COMMAND" >&2' ERR
set -Eeuo pipefail

# {{ range (glob (joinPath .chezmoi.sourceDir (.chezmoi.sourceFile | dir) "**/*")) }}
#   {{ if and (not (. | stat).isDir) (not (regexMatch ".css$" .)) }}
#     {{ include . | sha256sum }}
#   {{ end }}
# {{ end }}

# Reload waybar config
killall -SIGUSR2 waybar
