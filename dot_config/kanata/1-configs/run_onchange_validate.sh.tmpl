#!/bin/bash
trap 'echo "❌ Error on line $LINENO: $BASH_COMMAND" >&2' ERR
set -Eeuo pipefail

# {{ range (glob (joinPath .chezmoi.sourceDir (.chezmoi.sourceFile | dir) "**/*.kbd")) }}
#   {{ include . | sha256sum }}
# {{ end }}

# {{ range (glob (joinPath .chezmoi.sourceDir (.chezmoi.sourceFile | dir) "*.kbd")) }}
    echo -e "\t>>> Validating {{ . | base }}:"
    kanata --check --cfg {{ . }}
# {{ end }}
