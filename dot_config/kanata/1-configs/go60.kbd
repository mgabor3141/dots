(defcfg
  ;; Can't do include or exclude here because then `register_device` fails in kanata

  log-layer-changes no
  process-unmapped-keys yes
)

(include shared/common.kbd)

(defsrc)

(deflayermap (base-layer)
  lmet @wm-modifiers
  lctl lmet
)

(deflayermap (extend))
