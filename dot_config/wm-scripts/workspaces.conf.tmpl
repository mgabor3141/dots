# Workspace definitions — shared between aerospace and niri.
# Keep in sync with keybindings in aerospace.toml / niri config.kdl.
#
# Aerospace uses two-character names (first char = sort order, second = label).
# Niri uses dynamic "N-label" workspaces for editor slots (created by event daemon).

{{- if eq .chezmoi.os "darwin" }}
ALL_WORKSPACES="1A 2Q 3W 4T 51 62 73 84 95"
NUMBERED_WORKSPACES="51 62 73 84 95"
{{- else }}
STATIC_WORKSPACES="A Q W T"
MAX_EDITOR_SLOT=5
{{- end }}
