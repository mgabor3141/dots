# plist preferences

A reboot may be required for applied changes to take effect.

To add new files:

```console
plutil -convert xml1 ~/Library/Preferences/file.plist
chezmoi add ~/Library/Preferences/file.plist
```
