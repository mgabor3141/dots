---
name: pi-dev
description: >
  Use when working on pi itself — its extensions, SDK, themes, skills, TUI, or configuration.
---

# Pi Development

Find the package root with `PI_ROOT="$(dirname "$(readlink -f "$(which pi)")")/.."`  — it contains `README.md`, `docs/`, and `examples/`.

Read the relevant docs completely and follow `.md` cross-references before implementing.
