[Service]
# Allow user services (PipeWire) to set nice -11 and lock memory so module-rt
# uses direct SCHED_FIFO:88 instead of falling back to RTKit (capped at SCHED_RR:20).
# Without this, audio stutters occur under CPU load (e.g. Discord screen sharing).
LimitNICE=-11
LimitMEMLOCK=infinity
