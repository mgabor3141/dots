#!/usr/bin/env python3
"""Thin wrapper that runs the pr_status package from the same directory."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from pr_status.__main__ import main
main()
