#!/bin/bash
trap 'echo "❌ Error on line $LINENO: $BASH_COMMAND" >&2' ERR
set -Eeuo pipefail

echo "Installing FluidVoice..."

hdiutil attach fluid.dmg
echo -e "\tIf this errors, make sure full disk access is enabled for the terminal"
sudo rsync -a "/Volumes/FluidVoice Installer/FluidVoice.app" /Applications/
hdiutil detach "/Volumes/FluidVoice Installer"
